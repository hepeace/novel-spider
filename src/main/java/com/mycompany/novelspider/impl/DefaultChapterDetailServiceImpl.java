package com.mycompany.novelspider.impl;

import com.mycompany.novelspider.interfaces.ChapterDetailService;
import com.mycompany.novelspider.model.ChapterDetail;

public class DefaultChapterDetailServiceImpl extends AbstractChapterDetailServiceImpl implements ChapterDetailService {
    @Override
    public ChapterDetail getChapterDetail(String url) {
        return super.getChapterDetail(url);
    }
}
